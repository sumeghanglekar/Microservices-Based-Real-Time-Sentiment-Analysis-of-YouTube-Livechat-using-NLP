version: '3.8'
services:
  backend:
    build:
      context: .
      dockerfile: DockerFile
    ports:
      - 8002:8501
    volumes:
      - .:/app
networks:
  default:
    external:
      name: kafka-docker_default